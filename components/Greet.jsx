import React from 'react';
import {View, Text} from 'react-native';

const Greet = props => {
    return (
        <View>
            <Text>Welcome {this.props.waht}</Text>
        </View>
    );
};

export default Greet;

import React from 'react';
import {
    Text,
    ScrollView,
} from 'react-native';

export default function TextMessages() {
    return (
        <ScrollView>
            <Text>Zpráva</Text>
        </ScrollView>
    );
}
